import { StyleSheet, Text, SafeAreaView, Button } from 'react-native';

const Menu=({navigation})=>{
   return(
    <SafeAreaView>
     <Text>Menu</Text>
    </SafeAreaView>
   ) 
}

export default Menu;